# 6. Формат сообщений с информацией о галерее

**Приоритет:** Средний (UI)

## Описание
Обновление формата сообщений с изображением согласно MVP: обычное сообщение и сообщение при достижении порога 33.3%.

## Требования из MVP

### Обычное сообщение:
```
[Изображение]

📊 Галерея: "Название_галереи"
Вес: 2.4 | ⭐⭐⭐☆☆ (3.2/5.0)
Прогресс: [██████░░░░] 60% (12/20)

[👍] [👎]
```

### При достижении порога 33.3%:
```
Галерея: "Название_галереи"
Прогресс: [██████░░░░] 60% (12/20)

• Всего изображений: 20
• Получили "+": 5
• Получили "-": 12 (60%)
• Без оценки: 3

[👍] [👎]
[🚫 Исключить "Название_галереи"]
```

## Задачи

### 6.1. Проверка достижения порога
**Файл:** `bot/telegram_handler.py`

Добавить метод:
- `_should_show_threshold_notification(gallery_id)` - проверка, нужно ли показать уведомление о пороге
- Вызывает `voting_manager.check_exclusion_threshold()` для проверки порога
- Проверяет флаг `threshold_notification_shown` в БД
- Возвращает True, если достигнут порог И уведомление еще не показывалось
- Пороги проверяются в `voting.py` (задача 7)

### 6.2. Форматирование обычного сообщения
**Файл:** `bot/telegram_handler.py`

Модифицировать `_format_caption()`:
- Использовать формат из MVP для обычного сообщения
- Включать информацию о галерее, весе, рейтинге, прогресс-бар

### 6.3. Форматирование сообщения с порогом
**Файл:** `bot/telegram_handler.py`

Добавить метод:
- `_format_threshold_caption(image, gallery_stats)` - форматирование сообщения при достижении порога
- Использовать формат из MVP
- Включать детальную статистику

### 6.4. Добавление кнопки исключения
**Файл:** `bot/telegram_handler.py`

Модифицировать `_send_random_photo()`:
- Проверить достижение порога через `_check_exclusion_threshold()`
- Если порог достигнут, добавить кнопку исключения в клавиатуру
- Callback: `exclude_gallery_{gallery_id}`

### 6.5. Обновление подписи после голосования
**Файл:** `bot/telegram_handler.py`

В `handle_vote_callback()`:
- После обработки голоса проверить достижение порога
- Если достигнут, отредактировать сообщение с новым форматом и кнопкой исключения

## Тестирование
- Проверить формат обычного сообщения
- Проверить формат сообщения при достижении порога
- Проверить появление кнопки исключения
- Проверить пороги для 1, 2, 3+ изображений

## Зависимости
- Задача 2: Статистика галереи
- Задача 5: Прогресс-бар в подписи
- Задача 7: Пороги исключения

## Безопасность релиза

### ✅ Можно релизить после этой задачи

**Условия для безопасного релиза:**
1. Формат сообщений обновляется постепенно (только если есть статистика)
2. Если статистики нет, используется упрощенный формат (как раньше)
3. Кнопка исключения появляется только если порог достигнут (задача 7)
4. Если задача 7 не реализована, кнопка не показывается

**Fallback-механизмы:**
- Если статистики нет → упрощенный формат подписи (как раньше)
- Если веса нет → не показывать вес в подписи
- Если порог не достигнут → обычный формат (без кнопки исключения)
- Если задача 7 не реализована → кнопка исключения не показывается

**Проверка перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Если статистики нет, подпись отображается (упрощенный формат)
- [ ] Если статистика есть, новый формат отображается корректно
- [ ] Кнопка исключения появляется только при достижении порога (если задача 7 реализована)

**Что НЕ сломается:**
- Отображение изображений (работает в любом случае)
- Голосование (работает как раньше)
- Все остальные функции

## Связанные задачи
- Задача 8: Кнопка исключения галереи
- Задача 9: Уведомления о достижении порога
