# Чек-лист для релиза Docker образа после каждой задачи

## Общий процесс релиза

### 1. Перед релизом

- [ ] Задача реализована и протестирована локально
- [ ] Все тесты из раздела "Тестирование" задачи пройдены
- [ ] Все проверки из раздела "Безопасность релиза" задачи выполнены
- [ ] Код закоммичен и запушен в репозиторий
- [ ] PR создан и смержен (если используется)

### 2. Сборка Docker образа

```bash
# Создать тег для версии
git tag v1.X.Y-task-N  # где N - номер задачи

# Собрать образ
docker build -t your-registry/bot:v1.X.Y-task-N .

# Или использовать Makefile (если настроен)
make build TAG=v1.X.Y-task-N
```

### 3. Тестирование в dev окружении

- [ ] Образ собран успешно
- [ ] Бот запускается без ошибок
- [ ] Основные функции работают (команда `/random`, голосование)
- [ ] Новая функциональность работает корректно
- [ ] Нет ошибок в логах
- [ ] Fallback-механизмы работают (если применимо)

### 4. Релиз в production

```bash
# Запушить образ в registry
docker push your-registry/bot:v1.X.Y-task-N

# Обновить docker-compose.yml в production
# Изменить image: your-registry/bot:v1.X.Y-task-N

# Перезапустить контейнер
docker-compose pull
docker-compose up -d
```

### 5. После релиза

- [ ] Мониторинг логов (первые 10-15 минут)
- [ ] Проверка работы основных функций
- [ ] Проверка работы новой функциональности
- [ ] Нет ошибок в логах
- [ ] Производительность в норме

## Чек-листы по задачам

### Задача 1: Система весов галерей

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Голосование работает (👍/👎)
- [ ] Веса обновляются при голосовании (проверить в БД)
- [ ] Существующие записи в `gallery_preferences` имеют `weight = 1.0`
- [ ] Новые галереи получают `weight = 1.0` по умолчанию

**После релиза:**
- [ ] Проверить логи на ошибки
- [ ] Проверить обновление весов при голосовании

---

### Задача 2: Статистика галереи

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Голосование работает
- [ ] При первом голосовании за галерею запрашивается количество изображений
- [ ] Статистика сохраняется в БД
- [ ] Если запрос к StashApp не удался, система продолжает работать

**После релиза:**
- [ ] Проверить сбор статистики при голосовании
- [ ] Проверить сохранение в БД

---

### Задача 3: Взвешенный случайный выбор галереи

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Если весов нет, используется старый метод (проверить логи)
- [ ] Если взвешенный выбор не удался, используется fallback
- [ ] Выбор изображений работает в обоих случаях

**После релиза:**
- [ ] Проверить работу выбора изображений
- [ ] Проверить логи на использование fallback (если весов нет)

---

### Задача 4: Алгоритм выбора изображения в галерее

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Выбор изображений работает (даже если категории пустые)
- [ ] Fallback на другую категорию работает корректно

**После релиза:**
- [ ] Проверить распределение выбора (70/20/10)
- [ ] Проверить работу fallback

---

### Задача 5: Прогресс-бар в подписи

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Если статистики нет, подпись отображается без прогресс-бара
- [ ] Если статистика есть, прогресс-бар отображается корректно
- [ ] Нет ошибок деления на ноль

**После релиза:**
- [ ] Проверить отображение прогресс-бара
- [ ] Проверить работу без статистики

---

### Задача 6: Формат сообщений

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Если статистики нет, подпись отображается (упрощенный формат)
- [ ] Если статистика есть, новый формат отображается корректно
- [ ] Кнопка исключения появляется только при достижении порога (если задача 7 реализована)

**После релиза:**
- [ ] Проверить формат подписей
- [ ] Проверить работу без статистики

---

### Задача 7: Пороги исключения

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Голосование работает
- [ ] Проверка порога работает корректно для разных случаев (1, 2, 3+ изображения)
- [ ] Нет ошибок деления на ноль
- [ ] Если статистики нет, система продолжает работать

**После релиза:**
- [ ] Проверить работу проверки порогов
- [ ] Проверить логи на ошибки

---

### Задача 8: Кнопка исключения

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Кнопка исключения появляется только при достижении порога
- [ ] Исключение галереи работает корректно
- [ ] Исключенные галереи не участвуют в выборе (проверить логи)
- [ ] Сообщение подтверждения отправляется

**После релиза:**
- [ ] Проверить работу исключения
- [ ] Проверить, что исключенные галереи не выбираются

---

### Задача 9: Уведомления о пороге

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Уведомление показывается один раз при достижении порога
- [ ] Уведомление не повторяется при повторном показе изображения
- [ ] Кнопка исключения остается после показа уведомления

**После релиза:**
- [ ] Проверить показ уведомлений
- [ ] Проверить, что уведомление не повторяется

---

### Задача 10: Команда /excluded

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/excluded` работает
- [ ] Если исключенных галерей нет, показывается корректное сообщение
- [ ] Если есть исключенные галереи, список отображается корректно
- [ ] Кнопки восстановления работают (если задача 11 реализована)

**После релиза:**
- [ ] Проверить работу команды
- [ ] Проверить отображение списка

---

### Задача 11: Команда /restore

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/restore {gallery_id}` работает
- [ ] Восстановление через кнопку работает
- [ ] Если галерея не найдена, показывается корректное сообщение
- [ ] Восстановленная галерея снова участвует в выборе

**После релиза:**
- [ ] Проверить работу восстановления
- [ ] Проверить, что восстановленные галереи снова доступны

---

### Задача 12: Команда /threshold

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/threshold` работает
- [ ] Если галерей с голосами нет, показывается корректное сообщение
- [ ] Если есть галереи, список отображается корректно
- [ ] Прогресс-бары отображаются правильно

**После релиза:**
- [ ] Проверить работу команды
- [ ] Проверить отображение списка

---

### Задача 13: Кэширование весов

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Команда `/random` работает
- [ ] Кэш работает (проверить логи на повторные запросы)
- [ ] Инвалидация кэша работает при обновлении веса
- [ ] Производительность улучшилась (опционально)

**После релиза:**
- [ ] Проверить работу кэша
- [ ] Проверить производительность

---

### Задача 14: Скрипт миграции

**Перед релизом:**
- [ ] Скрипт запускается без ошибок
- [ ] Скрипт корректно пересчитывает веса
- [ ] Скрипт не ломает существующие данные
- [ ] Dry-run режим работает корректно

**После релиза:**
- [ ] Запустить скрипт в production (опционально)
- [ ] Проверить пересчитанные веса

---

### Задача 15: Оптимизация производительности

**Перед релизом:**
- [ ] Бот запускается без ошибок
- [ ] Все функции работают как раньше
- [ ] Производительность улучшилась (опционально, проверить метрики)
- [ ] Индексы созданы корректно

**После релиза:**
- [ ] Проверить производительность
- [ ] Проверить работу всех функций

## Откат (Rollback)

Если что-то пошло не так после релиза:

```bash
# Откатиться на предыдущую версию
docker-compose down
# Изменить image в docker-compose.yml на предыдущую версию
docker-compose up -d
```

Или использовать теги:
```bash
docker pull your-registry/bot:v1.X.Y-previous
docker-compose up -d
```

## Мониторинг после релиза

- Проверять логи первые 15-30 минут после релиза
- Мониторить ошибки в логах
- Проверять работу основных функций
- Проверять производительность (если применимо)
