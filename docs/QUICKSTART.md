# Быстрый старт 🚀

## 3 шага для запуска бота

### 1️⃣ Получите Telegram Bot Token

```
1. Откройте @BotFather в Telegram
2. Отправьте: /newbot
3. Следуйте инструкциям
4. Сохраните токен (1234567890:ABCdef...)
```

Узнайте свой Telegram ID через [@userinfobot](https://t.me/userinfobot)

### 2️⃣ Настройте конфигурацию

```bash
# Скопируйте пример конфигурации
cp config.example.yml config.yml

# Отредактируйте config.yml
nano config.yml
```

Минимальная конфигурация:

```yaml
telegram:
  bot_token: "ВАШ_ТОКЕН_ОТ_BOTFATHER"
  allowed_user_ids: [ВАШ_TELEGRAM_ID]

stash:
  api_url: "http://localhost:9999/graphql"  # Измените если нужно

scheduler:
  enabled: true
  cron: "0 10 * * *"  # Каждый день в 10:00
  timezone: "Europe/Moscow"

history:
  avoid_recent_days: 30

database:
  path: "/data/sent_photos.db"
```

### 3️⃣ Запустите бот

#### Docker (рекомендуется):

```bash
# Создайте директорию для данных
mkdir -p data logs

# Запустите
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

#### Локально:

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите
python -m bot.main
```

## ✅ Проверка работы

Откройте вашего бота в Telegram и отправьте:

```
/start   - Приветствие
/random  - Случайное фото
/stats   - Статистика
```

## 🔧 Настройка расписания

Примеры cron выражений:

```yaml
"0 10 * * *"     # Каждый день в 10:00
"0 9,21 * * *"   # В 9:00 и 21:00
"0 12 * * 1-5"   # Пн-Пт в 12:00
"0 */3 * * *"    # Каждые 3 часа
```

## 🆘 Проблемы?

**Бот не отвечает:**
- Проверьте, что ваш ID в `allowed_user_ids`
- Проверьте логи: `docker-compose logs -f`

**Не подключается к StashApp:**
- Проверьте `api_url` в config.yml
- Для Docker используйте IP адрес, не localhost

**Планировщик не работает:**
- Убедитесь что `enabled: true`
- Проверьте cron выражение и timezone

## 📚 Полная документация

- [README.md](README.md) - Подробное описание
- [DEPLOYMENT.md](DEPLOYMENT.md) - Инструкции по развертыванию
- [CHANGELOG.md](CHANGELOG.md) - История изменений

## 🎯 Полезные команды

```bash
make up       # Запустить
make down     # Остановить
make logs     # Логи
make restart  # Перезапустить
make backup   # Резервная копия БД
make stats    # Статистика
```

---

**Готово! Наслаждайтесь случайными фото из StashApp! 📸**
