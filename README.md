# StashApp Telegram Bot

Telegram бот для автоматической отправки случайных фотографий из вашей коллекции StashApp.

## 🌟 Возможности

- 📸 Отправка случайных фото по команде `/random` или кнопке "💕 Random"
- ⏰ Автоматическая отправка по расписанию (настраиваемый cron)
- 🔄 Избегание повторов (не отправляет фото из недавней истории)
- 📊 Статистика отправленных фото и голосований
- 🗳️ Система голосования (👍/👎) с автоматическим обновлением рейтингов в StashApp
- ⚖️ Система весов галерей для умного выбора контента
- 📈 Отслеживание предпочтений по перформерам и галереям
- 🚫 Исключение галерей при достижении порога негативных оценок
- ⚡ Предзагрузка изображений для быстрой отправки
- 💾 Кэширование file_id для оптимизации отправки
- 🔐 Контроль доступа по whitelist Telegram ID
- 🐳 Простое развертывание через Docker

## 📋 Требования

- Python 3.11+ (для локального запуска)
- Docker и Docker Compose (для контейнеризации)
- Работающий экземпляр StashApp
- Telegram Bot Token (от [@BotFather](https://t.me/BotFather))

## 🚀 Быстрый старт

### 1. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и сохраните полученный токен
4. Узнайте свой Telegram ID через [@userinfobot](https://t.me/userinfobot)

### 2. Настройка конфигурации

```bash
# Скопируйте пример конфигурации
cp config.example.yml config.yml

# Отредактируйте config.yml и укажите:
# - Telegram Bot Token
# - Ваш Telegram ID
# - URL StashApp API
# - Расписание отправки
```

### 3. Запуск через Docker (рекомендуется)

```bash
# Создайте директории для данных
mkdir -p data logs

# Запустите контейнер
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### 4. Режим webhook vs polling

По умолчанию бот работает в режиме **polling** (сам опрашивает Telegram).
Для продакшена удобнее использовать **webhook** c HTTPS на роутере/прокси.

- **polling**:
  - проще в настройке, не требует внешнего HTTPS
  - достаточно проброса доступа к интернету из контейнера/хоста
- **webhook**:
  - Telegram шлёт обновления на ваш HTTPS URL
  - требуется домен и валидный SSL (например, KeenDNS + Let's Encrypt на Keenetic)
  - бот внутри контейнера слушает HTTP, SSL терминируется на роутере/прокси

Включение webhook режима настраивается в `config.yml`:

```yaml
telegram:
  # ...
  webhook:
    enabled: true
    url: "https://yourdomain.keenetic.pro/webhook"
    port: 8443
    secret_token: "SOME_SECRET"  # опционально, но рекомендуется
    listen_address: "0.0.0.0"
```

В `docker-compose.yml` порт 8443 внутри контейнера по умолчанию проброшен наружу,
а переменная окружения `WEBHOOK_PORT` позволяет переопределить внешний порт при необходимости.

### 5. Запуск локально (для разработки)

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python -m bot.main
```

## ⚙️ Конфигурация

### config.yml

Основной конфигурационный файл. Пример:

```yaml
telegram:
  bot_token: "YOUR_BOT_TOKEN"
  allowed_user_ids: [123456789]
  # ID служебного канала для предзагрузки изображений (опционально)
  # Ускоряет отправку изображений через кэширование file_id
  cache_channel_id: -1001234567890  # опционально

stash:
  api_url: "http://stash:9999/graphql"
  api_key: ""  # опционально
  username: ""  # опционально, если требуется авторизация
  password: ""  # опционально, если требуется авторизация

scheduler:
  enabled: true
  cron: "0 10 * * *"  # каждый день в 10:00
  timezone: "Europe/Moscow"

history:
  avoid_recent_days: 30

database:
  path: "/data/sent_photos.db"

telegram:
  # ...
  webhook:
    enabled: false
    url: "https://yourdomain.keenetic.pro/webhook"
    port: 8443
    secret_token: ""
    listen_address: "0.0.0.0"
```

### Примеры cron расписаний

- `0 10 * * *` - каждый день в 10:00
- `0 9,21 * * *` - каждый день в 9:00 и 21:00
- `0 12 * * 1-5` - с понедельника по пятницу в 12:00
- `*/30 * * * *` - каждые 30 минут
- `0 */3 * * *` - каждые 3 часа

## 🤖 Команды бота

- `/start` - Приветствие и информация о боте
- `/help` - Справка по командам
- `/random` - Получить случайное фото из коллекции
- `/stats` - Показать статистику отправленных фото и голосований
- `/preferences` - Показать ваши предпочтения (любимые/нелюбимые перформеры и галереи)

### Интерактивные функции

- **Кнопка "💕 Random"** - постоянная кнопка внизу экрана для быстрого запроса фото
- **Голосование 👍/👎** - под каждым фото есть кнопки для оценки:
  - 👍 - ставит рейтинг 5/5 фото и запоминает перформеров/галерею
  - 👎 - ставит рейтинг 1/5 и фильтрует похожий контент
  - После голосования за последнее отправленное фото автоматически отправляется следующее фото
- **Кнопка исключения галереи 🚫** - появляется при достижении порога негативных оценок (≥33.3% минусов). Позволяет исключить галерею из ротации одним нажатием

## 🐳 Развертывание на TrueNAS Scale

### Вариант A: Через Custom App

1. Откройте Apps в TrueNAS Scale
2. Нажмите "Discover Apps" → "Custom App"
3. Заполните параметры:
   - **Application Name**: `stash-telegram-bot`
   - **Image Repository**: соберите образ локально или используйте registry
   - **Image Tag**: `latest`
4. В разделе **Storage**:
   - Добавьте Host Path для `/config` → ваш `config.yml`
   - Добавьте Host Path для `/data` → директория для БД
5. В разделе **Environment Variables**:
   - `TELEGRAM_BOT_TOKEN`: ваш токен
   - `TZ`: `Europe/Moscow`
6. Сохраните и запустите

### Вариант B: Через SSH и docker-compose

```bash
# Подключитесь к TrueNAS по SSH
ssh root@your-truenas-ip

# Создайте директорию для проекта
mkdir -p /mnt/pool/apps/stash-telegram-bot
cd /mnt/pool/apps/stash-telegram-bot

# Скопируйте файлы проекта (используйте scp или git clone)
# Создайте config.yml с вашими настройками

# Запустите
docker-compose up -d

# Проверьте статус
docker-compose ps
docker-compose logs -f
```

## 🔧 Подключение к StashApp в Docker

Если StashApp уже запущен в Docker, добавьте бота в ту же сеть:

```yaml
# docker-compose.yml
services:
  stash-telegram-bot:
    # ...
    networks:
      - stash-network

networks:
  stash-network:
    external: true
    name: stash_default  # имя сети StashApp
```

## 🎯 Система голосования и весов галерей

Бот использует умную систему выбора контента на основе ваших предпочтений:

### Как работает голосование

1. **Оценка фото**: При нажатии 👍 или 👎:
   - Рейтинг изображения обновляется в StashApp (5/5 для 👍, 1/5 для 👎)
   - Предпочтения по перформерам сохраняются в базе
   - Предпочтения по галерее сохраняются и обновляется вес галереи

2. **Система весов галерей**:
   - Каждая галерея имеет вес, который влияет на вероятность выбора
   - При 👍 вес увеличивается (коэффициент k=0.2)
   - При 👎 вес уменьшается (коэффициент k=0.2)
   - Галереи с большим весом выбираются чаще

3. **Умный выбор изображений**:
   - Выбор галереи происходит взвешенным случайным образом
   - Внутри галереи приоритет: 70% неоцененные, 20% с "+", 10% с "-"
   - Исключенные галереи не участвуют в выборе

4. **Пороги исключения**:
   - Галерея с 1-2 изображениями: 1 минус → порог достигнут
   - Галерея с 3+ изображениями: ≥33.3% минусов → порог достигнут
   - При достижении порога показывается уведомление и кнопка исключения
   - Исключенные галереи не участвуют в выборе изображений

### Формат сообщений

Под каждым фото отображается:

- 👤 Перформеры
- 📊 Название галереи
- Вес галереи и рейтинг (⭐⭐⭐☆☆)
- Прогресс-бар процента минусов: `[██████░░░░] 60% (12/20)`

При достижении порога показывается детальная статистика галереи.

## 📊 Структура проекта

```text
stash-telegram-bot/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Точка входа
│   ├── config.py            # Управление конфигурацией
│   ├── stash_client.py      # Клиент StashApp GraphQL API
│   ├── telegram_handler.py  # Обработчики команд бота
│   ├── scheduler.py         # Планировщик отправки
│   ├── voting.py            # Менеджер голосования
│   ├── performance.py       # Таймеры производительности
│   └── database/            # Модули работы с БД
│       ├── __init__.py
│       ├── base.py          # Базовая инициализация БД
│       ├── sent_photos.py    # История отправленных фото
│       ├── votes.py          # Голосования
│       ├── preferences.py    # Предпочтения (перформеры, галереи)
│       ├── statistics.py     # Статистика галерей
│       └── weights.py        # Веса галерей
├── docs/                     # Документация
│   └── gallery_weight_system/ # Документация системы весов
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── config.example.yml
└── README.md
```

## 🔐 Безопасность

- ✅ Telegram Bot Token хранится в переменных окружения
- ✅ Доступ ограничен whitelist пользователей по Telegram ID
- ✅ StashApp API доступен только внутри Docker сети
- ✅ Бот работает от непривилегированного пользователя
- ✅ Конфигурационные файлы добавлены в `.gitignore`

## 🐛 Отладка

### Просмотр логов

```bash
# Docker
docker-compose logs -f stash-telegram-bot

# Локально
tail -f bot.log
```

### Проверка подключения к StashApp

```bash
# Внутри контейнера
docker exec -it stash-telegram-bot python -c "
from bot.config import load_config
from bot.stash_client import StashClient
import asyncio

async def test():
    config = load_config('/config/config.yml')
    async with StashClient(config.stash.api_url, config.stash.api_key) as client:
        result = await client.test_connection()
        print(f'Connection OK: {result}')

asyncio.run(test())
"
```

### Типичные проблемы

**Проблема**: Бот не отвечает на команды

- ✅ Проверьте, что ваш Telegram ID в `allowed_user_ids`
- ✅ Убедитесь, что бот запущен (`docker-compose ps`)
- ✅ Проверьте логи на наличие ошибок

**Проблема**: Не удается подключиться к StashApp

- ✅ Проверьте URL в `config.yml`
- ✅ Убедитесь, что StashApp доступен из контейнера
- ✅ Проверьте настройки сети Docker

**Проблема**: Фото не отправляются по расписанию

- ✅ Убедитесь, что `scheduler.enabled: true`
- ✅ Проверьте правильность cron выражения
- ✅ Проверьте временную зону в конфигурации

## 📝 Дополнительные возможности

### Планируемые улучшения

- [ ] Команды `/excluded`, `/restore`, `/threshold` для управления исключенными галереями
- [ ] Фильтрация по тегам через команду `/random [тег]`
- [ ] Фильтрация по минимальному рейтингу
- [ ] Webhook вместо polling для Telegram
- [ ] Отправка альбомов (несколько фото)
- [ ] Уведомления администратору при ошибках
- [ ] Web интерфейс для управления

### Документация

Подробная документация по системе весов галерей находится в папке [`docs/gallery_weight_system/`](docs/gallery_weight_system/).

## 📄 Лицензия

MIT License

## 🤝 Вклад в проект

Буду рад вашим pull request и issue!

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Отладка" в этом README
2. Просмотрите логи бота
3. Создайте issue с описанием проблемы и логами

---

## 📝 Примечание

**Создано с ❤️ для удобной работы с StashApp**
